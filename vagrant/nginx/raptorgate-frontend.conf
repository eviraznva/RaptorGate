user www-data;
worker_processes auto;
pid /run/raptorgate-frontend-nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

events {
  worker_connections 1024;
}

http {
  sendfile on;
  tcp_nopush on;
  types_hash_max_size 2048;

  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  access_log /var/log/raptorgate/frontend/access.log;
  error_log /var/log/raptorgate/frontend/error.log warn;

  map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
  }

  server {
    listen 192.168.56.254:443 ssl;
    server_name _;

    ssl_certificate /resources/backend/devCerts/cert.pem;
    ssl_certificate_key /resources/backend/devCerts/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    root /resources/frontend/dist;
    index index.html;

    location = /api {
      return 308 /api/;
    }

    location /api/ {
      proxy_pass https://127.0.0.1:3000/;
      proxy_ssl_verify off;
      proxy_http_version 1.1;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Port 443;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection $connection_upgrade;
    }

    location / {
      try_files $uri $uri/ /index.html;
    }
  }

  # Issue 7: portal captive na interfejsie h1. Wystawiamy SPA tylko pod
  # /portal* oraz proxy do /api/identity/*. Wszystko inne (admin SPA, auth,
  # config) zostaje na vhoscie mgmt 192.168.56.254.
  server {
    listen 192.168.10.254:443 ssl;
    server_name _;

    ssl_certificate /resources/backend/devCerts/cert.pem;
    ssl_certificate_key /resources/backend/devCerts/key.pem;
    ssl_protocols TLSv1.2 TLSv1.3;

    root /resources/frontend/dist;
    index index.html;

    location = / {
      return 302 /portal/login;
    }

    location = /api/identity {
      return 308 /api/identity/;
    }

    location /api/identity/ {
      proxy_pass https://127.0.0.1:3000/identity/;
      proxy_ssl_verify off;
      proxy_http_version 1.1;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto https;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Port 443;
    }

    location /portal {
      try_files $uri $uri/ /index.html;
    }

    location /assets/ {
      try_files $uri =404;
    }

    location = /favicon.ico {
      try_files $uri =404;
    }

    location / {
      return 404;
    }
  }
}

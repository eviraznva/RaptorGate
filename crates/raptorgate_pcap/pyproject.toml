[build-system]
requires = ["maturin>=1.7"]
build-backend = "maturin"

[project]
name = "raptorgate-pcap"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
module-name = "raptorgate_pcap"
features = ["extension-module"]
